openapi: 3.1.0
info:
  title: NEAR Data API - Last Final Block
  version: 1.0.0
servers:
  - url: https://mainnet.neardata.xyz
    description: Mainnet
  - url: https://testnet.neardata.xyz
    description: Testnet
paths:
  /v0/last_block/final:
    get:
      operationId: get_last_block_final
      summary: Redirect to the latest finalized block
      tags:
        - redirects
      parameters:
        - $ref: "../components.yaml#/components/parameters/ApiKeyParam"
      responses:
        "200":
          description: Block document returned by clients that automatically follow the redirect
          content:
            application/json:
              schema:
                $ref: "../components.yaml#/components/schemas/BlockDocument"
        "302":
          description: Redirects to the latest finalized block URL
          headers:
            Location:
              schema:
                type: string
