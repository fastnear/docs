openapi: 3.1.0
info:
  title: KV FastData API - GET History by Exact Key
  version: 1.0.0
servers:
  - url: https://kv.main.fastnear.com
    description: Mainnet
  - url: https://kv.test.fastnear.com
    description: Testnet
paths:
  /v0/history/{current_account_id}/{predecessor_id}/{key}:
    get:
      operationId: get_history_key
      summary: Fetch historical rows for one exact key
      tags:
        - history
      parameters:
        - name: current_account_id
          in: path
          required: true
          schema:
            type: string
        - name: predecessor_id
          in: path
          required: true
          schema:
            type: string
        - name: key
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Historical rows for the selected key
          content:
            application/json:
              schema:
                $ref: "../components.yaml#/components/schemas/ListResponse"
        "500":
          description: Scylla query failure
          content:
            application/json:
              schema:
                $ref: "../components.yaml#/components/schemas/ApiError"
