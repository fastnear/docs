openapi: 3.1.0
info:
  title: FastNEAR API - V0 Account NFTs
  version: 1.0.0
servers:
  - url: https://api.fastnear.com
    description: Mainnet
  - url: https://test.api.fastnear.com
    description: Testnet
paths:
  /v0/account/{account_id}/nft:
    get:
      operationId: account_nft_v0
      summary: Lookup NFT contracts for an account
      tags:
        - v0
      parameters:
        - name: account_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: NFT contract IDs for the requested account
          content:
            application/json:
              schema:
                type: object
                required:
                  - account_id
                  - contract_ids
                properties:
                  account_id:
                    type: string
                  contract_ids:
                    type: array
                    items:
                      type: string
              example:
                account_id: collector.near
                contract_ids:
                  - nft.genadrop.near
                  - mintbase1.near
        "400":
          description: Invalid account ID
          content:
            application/json:
              schema:
                type: string
        "500":
          description: Redis or upstream failure
          content:
            application/json:
              schema:
                type: string
